import { SubRouteStub } from "@/components/chart/SubRouteStub";

export default function FonDagitimPage() {
  return (
    <SubRouteStub
      title="Varlık Dağılımı"
      description="Fonun portföyündeki varlık sınıfı dağılımı (hisse / bono / repo / döviz / altın vb.) ve son aydaki değişimler burada görselleştirilecek."
      phase="Faz 10.5 — TEFAS portföy dağılımı entegrasyonu"
    />
  );
}
